node_modules
dist
.env
.env.local
.env.*.local
.DS_Store
*.log

# raw spotify export stays local (huge + private)
data/raw/
data/raw-*/
# public/data.json (and per-user variants) are committed so GH Actions can build without raw
